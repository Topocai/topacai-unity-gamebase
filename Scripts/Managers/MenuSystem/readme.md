# Topacai Menu system

This is an unfinished menu system that could used not for UI menus but for all type of menus
providing a `page-based` system or `backwads` callback for any menu element using an unique input
